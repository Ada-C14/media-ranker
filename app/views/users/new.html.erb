<%= render partial: 'layouts/form_errors', locals: {
    model: @user } %>

<%= form_with model: @user, local: true do |f| %>
  <h2>Sign Up</h2>
  <p><%= f.label :username %>
    <%= f.text_field :username%>
  </p>
  <p><%= f.submit "Log in", class: "btn btn-primary" %></p>
<% end %>