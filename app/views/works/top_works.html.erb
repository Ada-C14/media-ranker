
<section class="spotlight">

  <h2 class="spotlight-header">
    <span class="spotlight-header-prefix">Media Spotlight:</span>
    <%= link_to @spotlight.title, work_path(@spotlight), class: 'spotlight-link-to' %><span> by </span><%= @spotlight.creator %>
  </h2>

  <p class="spotlight-description"><%= @spotlight.description %></p>
</section>

<hr class="root-break"/>

<section class="top-ten-container">

  <section class="ten-list-container">

    <h2 class="ten-header">Top Movies</h2>

    <ul class="list-group top-ten-list">
      <% @top_movies.each do |movie| %>
        <li>

          <h4>
            <%= link_to movie.title, work_path(movie.id) %>
            <small class="top-ten-creator">by <%= movie.creator %></small>
          </h4>
          <p><%= link_to movie.votes.count %> votes</p>
        </li>
      <% end %>
    </ul>

  </section>

  <section class="ten-list-container">

    <h2 class="ten-header">Top Books</h2>

    <ul class="list-group top-ten-list">
      <% @top_books.each do |book| %>
        <li>

          <h4>
            <%= link_to book.title, work_path(book.id) %>
            <small class="top-ten-creator">by <%= book.creator %></small>
          </h4>
          <p><%= link_to book.votes.count %> votes</p>
        </li>
      <% end %>
    </ul>
  </section>

  <section class="ten-list-container">

    <h2 class="ten-header">Top Albums</h2>

    <ul class="list-group top-ten-list">
      <% @top_albums.each do |album| %>
        <li>

          <h4>
            <%= link_to album.title, work_path(album.id) %>
            <small class="top-ten-creator">by <%= album.creator %></small>
          </h4>
          <p><%= link_to album.votes.count %> votes</p>
        </li>
      <% end %>
    </ul>
  </section>

</section>

