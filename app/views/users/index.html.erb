<h1>List of Users</h1>

<div>
  <ul>
    <% @users.each do |user|  %>
      <li>
        Username: <%= link_to user.username, user_path(user.id) %>
        Votes: <%= user.votes.count %>
        Joined: <%= user.created_at %>
      </li>

    <% end %>
  </ul>
</div>