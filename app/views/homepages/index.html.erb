<h2>Media Spotlight: <%= Work.spotlight.title %> by <%= Work.spotlight.creator %></h2>
<p><%= rand(1..50) %> votes - <%= Work.spotlight.description %></p>

<section>
  <% @top_ten_movies = Work.top_ten("movie") %>
  <% if @top_ten_movies.empty? %>
    <p>No top ten for this category</p>
  <% end %>

  <ul>
    <% @top_ten_movies.each do |movie| %>
      <li>
        <h4>
          <%= link_to movie.title, work_path(movie) %>
          <small><%= "By: #{movie.creator}" %></small>
        </h4>
        <p><%= "#{rand(0..30)} votes" %></p>
      </li>
    <% end %>
  </ul>

  <% @top_ten_books = Work.top_ten("book") %>
  <% if @top_ten_books.empty? %>
    <p>No top ten for this category</p>
  <% end %>

  <ul>
    <% @top_ten_books.each do |book| %>
      <li>
        <h4>
          <%= link_to book.title, work_path(book) %>
          <small><%= "By: #{book.creator}" %></small>
        </h4>
        <p><%= "#{rand(0..50)} votes" %></p>
      </li>
    <% end %>
  </ul>

  <% @top_ten_albums = Work.top_ten("album") %>
  <% if @top_ten_albums.empty? %>
    <p>No top ten for this category</p>
  <% end %>

  <ul>
    <% @top_ten_albums.each do |album| %>
      <li>
        <h4>
          <%= link_to album.title, work_path(album) %>
          <small><%= "By: #{album.creator}" %></small>
        </h4>
        <p><%= "#{rand(0..50)} votes" %></p>
      </li>
    <% end %>
  </ul>
</section>
