require "test_helper"

describe VotesController do
  describe "create" do
    it "does not create a vote if user is not logged in" do

    end

    it "creates a vote with logged in user" do

    end

    it "will not let a logged in user vote more than once on the same work" do

    end
  end
end
