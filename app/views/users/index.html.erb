<h1>All Users</h1>

<h3><%= button_to "Add a User", new_user_path, method: :get %></h3>
<div>
  <ul>
    <% @users.each do |user|  %>
      <li>
        <%= link_to user.title, user_path(user.id) %>
      </li>
    <% end %>
  </ul>
</div>