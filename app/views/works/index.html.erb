<h2>List of Works</h2>
  <nav>
    <%= button_to "View top media", root_path, method: :get %>
    <%= button_to "View all media", works_path, method: :get %>
    <%= button_to "Add new work", new_work_path, method: :get %>
    <% if session[:user_id] %>
      <%= button_to "Logout", logout_path, method: :post %>
    <%else %>
      <%=button_to "Log in", login_path, method: :get %>
    <%end %>
  </nav>

<section class= "Album">
  <h3>Albums:</h3>
  <% albums = Work.where(media: "album") %>
  <% if @albums.size > 0 %>
    <table class= "table">
      <thead>
      <tr>
        <th> Title: </th>
        <th> Creator: </th>
        <th> Publication: </th>
      </tr>
      </thead>
      <tbody>
      <% @albums.each do |album| %>
        <tr>
          <td><%= link_to album.title, work_path(album) %></td>
          <td><%= album.created_by %></td>
          <td><%= album.published %></td>
        </tr>
        <% end %>
    <%else %>
    <%end %>
    </tbody>
  </table>
</section>

<section class= "Book">
  <h3>Books:</h3>
  <% books = Work.where(media: "books") %>
  <% if @books.size > 0 %>
    <table class= "table">
      <thead>
      <tr>
        <th> Title: </th>
        <th> Creator: </th>
        <th> Publication: </th>
      </tr>
      </thead>
      <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= link_to book.title, work_path(book) %></td>
          <td><%= book.created_by %></td>
          <td><%= book.published %></td>
        </tr>
      <% end %>
  <%else %>
  <%end %>
  </tbody>
  </table>
</section>

<section class= "Movies">
  <h3>Movies:</h3>
  <% movies = Work.where(media: "movie") %>
  <% if @movies.size > 0 %>
    <table class= "table">
      <thead>
      <tr>
        <th> Title: </th>
        <th> Creator: </th>
        <th> Publication: </th>
      </tr>
      </thead>
      <tbody>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= link_to movie.title, work_path(movie) %></td>
          <td><%= movie.created_by %></td>
          <td><%= movie.published %></td>
        </tr>
      <% end %>
  <%else %>
  <%end %>
  </tbody>
  </table>
</section>

  <%= button_to "Add a New Media", new_work_path, method: :get %>