<h2>Edit This <%= @work.category.capitalize %></h2>
<%= render partial: "form", locals: { action_name: "Update Work", title: "#{@work.title}", category: "#{@work.category}", publication_year: "#{@work.publication_year}", description: "#{@work.description}", creator: "#{@work.creator}"} %>
