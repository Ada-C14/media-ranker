<h2> This is a great time to be an Adie </h2>
<h3> Works Page </h3>

<% if @works.length > 0 %>
  <ul>
    <% @works.each do |work| %>
    <li> <%= work[:title] %> by <%= work[:author] %></li>
    <% end %>
  </ul>
<% else %>
<h3> No works in the system! </h3>
<% end %>