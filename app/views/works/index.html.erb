<h2>List of Works</h2>

<%= render partial: "list_of_work", locals: { category: "Albums", works: @albums } %>

<%= render partial: "list_of_work", locals: { category: "Books", works: @books } %>

<%= render partial: "list_of_work", locals: { category: "Movies", works: @movies } %>

<%= link_to "View top media", root_path, class: "btn btn-secondary" %>
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary" %>
