<section>
  <h2><%= @work.title %></h2>
  <p>Created by: <%= @work.creator %></p>
  <p>Published: <%= @work.publication_year %>
  <p><%= @work.description %></p>
  <section>
    <%= link_to "Back to Media Ranks", works_path, class: "btn btn-primary" %>
    <%= link_to "Edit", edit_work_path(@work), class: "btn btn-primary" %>
    <%= link_to "Upvote", class: "btn btn-primary" %>
    <%= link_to "Delete", work_path(@work), data: {confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger" %>
  </section>
</section>
<section>
  <h3>[insert #] of Votes for this Album</h3>
  <%# add table of votes %>
</section>