<section class = "spotlight"><h1><span class ="media">Media Spotlight: </span>
  <%= link_to @media_spotlight.title, work_path(@media_spotlight)%>  by <%= @media_spotlight.creator %> </h1>
<p class = "spotlight_description"> <%=@media_spotlight.description %> </p>
</section>
<section class="largest-container">
<section class="top-ten-container">
  <h2 class="top-ten-header"> Top Movies</h2>
  <ul class="top-ten-list">
    <%# sample_ten = @works.where(category: "movie").sample(10) %>
<!--    # @works.where(category: "movie").top_ten-->
<!--    #<%# descending_list = @works.top_ten %>-->
    <%# find_cat = descending_list.where(category: "movie") %>
    <% @top_movies.each do |work| %>
      <li class = "list-group-item"> <%= link_to work.title, work_path(work) %> <small class="creator"> <br> by <%= work.creator%> </small></li>
    <%end %>
  </ul>
</section>

<section class="top-ten-container">
  <h2 class="top-ten-header"> Top Books</h2>
    <ul class="top-ten-list">
      <%# sample_ten = @works.where(category: "book").sample(10) %>
      <% @top_books.each do |work| %>
        <li class = "list-group-item"> <%= link_to work.title, work_path(work) %> <small class="creator"> <br> by <%= work.creator%> </small> <br></li>
      <%end %>
    </ul>
</section>

<section class="top-ten-container">
  <h2 class="top-ten-header"> Top Albums</h2>
    <ul class="top-ten-list">
      <%# sample_ten = @works.where(category: "album").sample(10) %>
      <% @top_albums.each do |work| %>
        <li class = "list-group-item"> <%= link_to work.title, work_path(work) %> <small class="creator"> <br> by <%= work.creator%></small><br></li>
      <%end %>
    </ul>
</section>
</section>
<!--<div class="row">-->
<!--  <div class="column"><h2>Top movies </h2>-->
    <%#  @works.each do |work| %>
      <%# if work.category == "movie" %>
    <%# sample_ten = @works.where(category: "movie").sample(10) %>
    <%# sample_ten.each do |work| %>
<!--    <b><%#= link_to work.title, work_path(work) %></b>  by <%#= work.creator%><br>-->
    <%# end %>

<!--  </div>-->
<!--  <div class="column"> <h2>Top Books</h2>-->
   <%#  sample_ten = @works.where(category: "book").sample(10) %>
    <%# sample_ten.each do |work| %>
<!--        <b><%#= link_to work.title, work_path(work) %></b>  by <%#= work.creator%><br>-->
      <%#end %>

<!--  </div>-->
<!--  <div class="column"><h2>Top Albums</h2>-->
    <%# sample_ten = @works.where(category: "album").sample(10)  %>
    <%# sample_ten.each do |work| %>
<!--        <b><%#= link_to work.title, work_path(work) %></b>  by <%#= work.creator%><br>-->
    <%# end %>

<!--  </div>-->
<!--</div>-->


