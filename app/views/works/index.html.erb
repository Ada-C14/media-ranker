<h2>List of Works</h2>

<section class="media-table"></section>

<%= render partial: "list", locals: { header: "Albums", works: @albums } %>

<%= render partial: "list", locals: { header: "Books", works: @books } %>

<%= render partial: "list", locals: { header: "Movies", works: @movies } %>

<%= link_to 'View top media', root_path, class:"btn btn-secondary" %>
<%= link_to 'Add a new work', new_work_path, class:"btn btn-primary" %>
