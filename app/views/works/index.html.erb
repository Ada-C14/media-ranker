<div class="album-table">
  <table class="table">
    <h3>List of Works</h3>
    <h4>Albums</h4>
      <tr>
        <th>Votes</th>
        <th>Title</th>
        <th>Created By</th>
        <th>Published</th>
        <th>Upvote</th>
      </tr>
      <% @albums.each do |album| %>
        <tr>
          <td><%= album.votes.count %></td>
          <td><%= link_to album.title, work_path(album.id) %></td>
          <td><%= album.creator %></td>
          <td><%= album.publication_year %></td>
          <td><%= link_to "Upvote", work_upvote_path(album.id), method: :post, class:"btn btn-primary" %></td>
      <% end %>
        </tr>
  </table>
</div>
<div class="book-table">
  <table class="table">
  <h4>Books</h4>
    <tr>
      <th>Votes</th>
      <th>Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Upvote</th>
    </tr>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.votes.count %></td>
        <td><%= link_to book.title, work_path(book.id) %></td>
        <td><%= book.creator %></td>
        <td><%= book.publication_year %></td>
        <td><%= link_to "Upvote", work_upvote_path(book.id), method: :post, class:"btn btn-primary" %></td>
    <% end %>
    </tr>
  </table>
</div>
<div class="movie-table">
  <table class="table">
  <h4>Movies</h4>
    <tr>
      <th>Votes</th>
      <th>Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Upvote</th>
    </tr>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.votes.count %></td>
        <td><%= link_to movie.title, work_path(movie.id) %></td>
        <td><%= movie.creator %></td>
        <td><%= movie.publication_year %></td>
        <td><%= link_to "Upvote", work_upvote_path(movie.id), method: :post, class:"btn btn-primary" %></td>
    <% end %>
    </tr>
  </table>
</div>

