<h2>List of Works</h2>

<%= render partial: "table", locals: { category: @albums, header: "Albums" } %>

<%= render partial: "table", locals: { category: @books, header: "Books" } %>

<%= render partial: "table", locals: { category: @movies, header: "Movies" } %>

<%= link_to "View top media", root_path, class: "btn btn-secondary" %>
<%= link_to "Add a new work", new_work_path, class: "btn btn-primary" %>