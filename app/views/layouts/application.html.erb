<!DOCTYPE html>
<html>
  <head>
    <title>MediaRanker</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

<!--    header-->
<header>

    <div>
      <h1>
        <%= link_to "Medcia Ranker", root_path %>
        | Ranking the Best of Everything
      </h1>


    </div>

<!--  header navbar-->
  <nav>
    <button></button>

    <div>
      <div>
        <ul>
          <li><%= link_to "view top media", root_path %></li>
          <li><%= link_to "view all media", works_path%></li>
          <li> <%= link_to "add a new work", new_work_path%></li>
          <li><%= link_to "view all users", users_path%></li>
          <% if session[:user_id] %>
            <% current_user = User.find_by(id: session[:user_id]) %>
            <%= link_to "Logged in as #{current_user.username}", users_path(user.id) %>
            <li><%= link_to "Logout", logout_path, method: :post %></li>
          <% else %>
            <li><%= link_to "Log in", login_path%></li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>
  <section class="flash-msg__section">
    <% flash.each do |name, message| %>
      <strong class="<%= name %>">
        <%= message %>
      </strong>
    <% end %>
  </section>

<!--  login button-->?


    <div>
      <%= yield %>
    </div>

  </body>

</html>


