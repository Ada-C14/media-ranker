<h3><%= header %></h3>
<table>
  <tr>
    <th>Votes</th>
    <th>Title</th>
    <th>Creator</th>
    <th>Published</th>
    <th>Upvote</th>
  </tr>
  <% works.each do |work| %>
    <tr>
      <td><%= work.votes.count %></td>
      <td><%= link_to "#{work.title}", work_path(work.id) %></td>
      <td><%= work.creator %></td>
      <td><%= work.publication_year %></td>
      <td><%= button_to "Upvote", work_upvote_path(work.id) %></td>
    </tr>
  <% end %>
</table>